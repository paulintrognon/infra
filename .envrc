# kubectl reads KUBECONFIG; the tofu helm/kubernetes providers read
# KUBE_CONFIG_PATH instead. Export the same path under both names so
# kubectl and tofu both find the cluster automatically inside the repo.
export KUBECONFIG=$HOME/.kube/paulin-config
export KUBE_CONFIG_PATH=$KUBECONFIG
